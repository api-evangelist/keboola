{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "BucketDetailRequest",
  "properties": {
    "include": {
      "description": "Comma-separated list of data to include. Allowed values: columns, metadata, columnMetadata, sourceMetadata, sourceColumnMetadata.",
      "type": "string",
      "nullable": true
    }
  },
  "type": "object"
}